#------------------------- Database -------------------------#
spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/bbdd_proyecto_miri_lucas
        username: proyecto
        password: proyecto
        #driver-class-name: org.postgresql.Driver
       # validation-query: SELECT 1
        #initialize: true

#------------------------- JPA -------------------------#
jpa:
    hibernate:
        ddl-auto: update
    show-sql: true
    #properties:
        #hibernate:
            #dialect: org.hibernate.dialect.PostgreSQLDialect
